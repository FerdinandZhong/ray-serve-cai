# assets/

Blueprint-standard media directory: diagrams, screenshots, sample media.

- `ray-serve-cai.png` — catalog card image for `METADATA.yaml` (add a
  screenshot of the deployed head app / Swagger UI here).
- Reprise walkthrough captures (see README → Demo).

The architecture diagrams live inline in `README.md` as Mermaid
(rendered natively by GitHub); export PNGs here only if the AMP catalog
card needs them.
